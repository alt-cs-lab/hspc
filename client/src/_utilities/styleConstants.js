/**
 * Constants for CSS Formatting
 */

export const selectStyles = {
    menu: (base) => ({
      ...base,
      zIndex: 100
    })
};

/**
 * Style for Purple Buttons
 */
export const buttonStyles = {
    backgroundColor: "#512888", 
    color: "white", 
    fontSize: 16
};